repos:
  - repo: local
    hooks:
      - id: update-requirements
        name: Update requirements.txt with pipreqs
        entry: pipreqs . --force
        language: system
        pass_filenames: false
